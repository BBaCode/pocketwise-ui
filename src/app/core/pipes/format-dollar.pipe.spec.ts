import { FormatDollarPipe } from './format-dollar.pipe';

describe('FormatDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
